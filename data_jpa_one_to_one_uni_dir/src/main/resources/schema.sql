CREATE TABLE IF NOT EXISTS TOPIC(
                                TOPIC_ID SERIAL NOT NULL,
                                TOPIC_NAME TEXT NOT NULL,
                                PRIMARY KEY(TOPIC_ID)
                                );
CREATE TABLE IF NOT EXISTS TOPIC_TYPE(
TOPIC_TYPE_ID SERIAL NOT NULL,

                                      );